1. Please find the sql file (drag.sql) and import it.
2. change the database configuration details under config.php file
3. set the base url (local path) inside configuration file.
example: "http://localhost/path/to/"
do not forget to keep the last slash (/)